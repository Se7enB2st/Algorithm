//Remove First and Last Character - Codewars

//Rule: Create a function that removes the first and last characters of a string. You're given one parameter.

function removeChar(str){
  return str.slice(1, -1);
};
